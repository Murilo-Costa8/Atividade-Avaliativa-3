let n = parseInt(prompt("Digite um numero inteiro positivo:"));

console.log("Soma dos números de " + n);

for (let i = 1; i <= n; i++);{
    let resultado = n + 1;
    console.log(n + " + " + i + " = " + resultado); 
}
